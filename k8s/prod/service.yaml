apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: gearworkssmp-com-prod
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
