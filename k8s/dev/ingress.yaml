apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web-dev
  namespace: gearworkssmp-com-dev
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`gearworkssmp-com.9m.se`)
      services:
        - name: web
          port: 80
      middlewares:
        - name: authentik-forwardauth
  tls:
    secretName: gearworkssmp-com-dev-tls
